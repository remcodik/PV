// Shared form-field classNames — kept as plain strings (rather than input
// components) since forms across the app mix native <input>, <select> and
// <textarea> with bespoke layouts.
export const labelClass = 'block text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1.5'

export const inputClass =
  'w-full px-3.5 py-2.5 border border-gray-300 rounded-md text-sm text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-ink-500/20 focus:border-ink-600 transition-colors'
